<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Sl Labs" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Sl Labs">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="main-header">
      <div class="container">
        <%= link_to "SL Labs", root_path, class: "logo" %>
        <nav>
          <% if authenticated? %>
            <%= link_to "Dashboard", repositories_path %>
            <%= link_to "New Repo", new_repository_path %>
            <%= button_to "Sign out", logout_path, method: :delete, class: "nav-link btn-link" %>
          <% else %>
            <%= link_to "Sign in", login_path %>
            <%= link_to "Sign up", signup_path, class: "btn-signup" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="container">
      <% flash.each do |type, message| %>
        <div class="flash flash-<%= type %>">
          <%= message %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <style>
      body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif; margin: 0; color: #24292f; }
      .container { max-width: 1012px; margin: 0 auto; padding: 0 16px; }
      .main-header { background: #24292f; color: white; padding: 16px 0; }
      .main-header .container { display: flex; align-items: center; justify-content: space-between; }
      .main-header .logo { color: white; text-decoration: none; font-weight: bold; font-size: 1.25rem; }
      .main-header nav { display: flex; gap: 1rem; align-items: center; }
      .main-header nav a { color: white; text-decoration: none; font-size: 0.9rem; }
      .main-header nav a:hover { opacity: 0.8; }
      .btn-signup { border: 1px solid white; padding: 4px 12px; border-radius: 6px; }
      .btn-link { background: none; border: none; color: white; cursor: pointer; font-size: 0.9rem; padding: 0; }
      .flash { padding: 16px; margin-top: 16px; border-radius: 6px; }
      .flash-notice { background: #dafbe1; color: #1a7f37; border: 1px solid #bcdec4; }
      .flash-alert { background: #ffebe9; color: #cf222e; border: 1px solid #fecaca; }
      main { padding-top: 2rem; }
    </style>
  </body>
</html>
